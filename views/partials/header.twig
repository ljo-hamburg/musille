{#
The main page header.
#}

<header class="wp-block-musille-header is-style-{{ page.header_style }}
               {% if page.header_background is null %}no-background{% endif %}"
        {% if page.header_background is not null %}
          style="background-image: url('{{ page.header_background.src }}');"
        {% endif %}
>
  <div class="overlay"></div>
  <div class="content">
    <h1 class="title">{{ page.title }}</h1>
    {% if page.subtitle %}
      <div class="subtitle">{{ page.subtitle }}</div>
    {% endif %}

    {% if function("is_singular", "post") %}
      <div class="meta">
        {{ __("%s by %s in %s", "musille")|format(
          post.date,
          '<a href="'~post.author.link~'">'~post.author.display_name~'</a>',
          '<a href="'~post.category.link~'">'~post.category.name~'</a>'
        ) }}
      </div>
    {% endif %}
  </div>
  {% if page.header_attribution %}
    <div class="attribution">{{ page.header_attribution }}</div>
  {% endif %}
</header>
